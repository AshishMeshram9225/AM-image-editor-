<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.amimageeditoreditlikepro">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/app_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:roundIcon="@mipmap/app_icon"
        android:theme="@style/Theme.App">

        <activity
            android:name="com.amimageeditoreditlikepro.SecondActivity"
            android:exported="false" />

        <activity
            android:name="com.amimageeditoreditlikepro.ThardActivity"
            android:exported="false" />

        <activity
            android:name="com.amimageeditoreditlikepro.MainActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

    </application>

</manifest>